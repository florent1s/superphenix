rookOperatorNamespace: "rook-system"
mapping: ""
clusters:
  # -- This is the configuration for the local Ceph cluster
  # It isn't needed for Decoupled Virtualization clusters, but must be
  # present on Hyperconverged or Decoupled Storage clusters.
  # If you're adding an override to connect another Ceph cluster, remember to copy
  # and paste this configuration to avoid losing the default values.
  - name: "spx-storage"
    local: true
    clusterID: "spx-storage"
    csi:
      secretNamespace: "spx-storage"
      rbdNodeSecretName: "rook-csi-rbd-node"
      rbdProvisionerSecretName: "rook-csi-rbd-provisioner"
    blockPools:
      - name: "spx-rbd-3x"
        storageClasses:
          - name: "spx-rbd-3x"
            fs: "ext4"
            default: true
#  - name: "spx-aq01-test01-storage01"
#    # -- Whether the cluster is local or remote
#    local: false
#    # -- Ceph cluster ID (FSID) or namespace for local clusters
#    # e.g e2a62ea1-6428-496e-a5bf-366936a8c833
#    clusterID: ""
#    # -- CSI-related configuration
#    csi:
#      # -- Namespace where the CSI secrets are located (defaults to cluster name)
#      secretNamespace: ""
#      # -- Name of the CSI node secret (defaults to cluster name)
#      rbdNodeSecretName: ""
#      # -- Name of the CSI provisioner secret (defaults to cluster name)
#      rbdProvisionerSecretName: ""
#    username: "spx-aq01-test01-virt01.local-block"
#    token: "test"
#    # -- Initial MON we will contact to retrieve the entire MON list
#    bootstrapMon:
#      # -- MON ID (must be a single letter)
#      id: ""
#      # -- IP of the MON
#      ip: ""
#      # -- Port of the MON
#      port: "6789"
#      # -- Protocol of the IP (IPv4 or IPv6)
#      protocol: IPv6
#    # -- Configuration used by Rook to check the health of the remote cluster and to retrieve the full MON list
#    healthCheck:
#      # -- Username of the remote healthcheck account on the storage cluster
#      username: "client.csi-health"
#      # -- Token associated with the account
#      token: ""
#    blockPools:
#      - name: "test"
#        # Create StorageClasses
#        storageClasses:
#          - name: "" # SC name will be "<cluster>.<name>" unless fullName is set. Name must be unique across every storageclass name in the cluster
#            # fullName: ""
#            # -- Data pool specified for erasure coded storage classes
#            dataPool: ""
#            # -- Filesystem type for the StorageClass
#            fs: "xfs"
#            default: true
#            reclaimPolicy: Delete
#            # -- Enable encryption for the StorageClass
#            encryption:
#              enabled: false
#              # -- Encryption passphrase (if KMS type is 'metadata')
#              passphrase: ""
#              # -- KMS type (metadata, vault, etc.)
#              kmsType: "metadata"
#              # -- Any other encryption parameters (e.g. encryptionKMSType if it differs from kmsType, etc.)
#              extraParameters: {}
#              # -- Any other KMS connection details for the ConfigMap
#              extraKMSParameters: {}
#    replicationClasses:
#      daily:
#        schedulingInterval: "24h"
#        schedulingStartTime: "22:00:00"
#    # -- RGW admin ops user credentials on the remote cluster.
#    # Required when objectStores is non-empty. Produces the hardcoded `rgw-admin-ops-user` Secret
#    # that the Rook operator uses to drive the RGW admin ops API for all CephObjectStores in
#    # this cluster's namespace.
#    rgwAdminOps:
#      accessKey: ""
#      secretKey: ""
#    # -- External RGW object stores. Each entry produces a CephObjectStore (external),
#    # an optional TLS Secret and one or more bucket StorageClasses usable with ObjectBucketClaims.
#    objectStores:
#      - name: "rgw" # CephObjectStore CR name (also used to derive the TLS secret name)
#        # -- External RGW endpoints. Each entry accepts either `ip` or `hostname`.
#        endpoints:
#          - ip: ""
#            # hostname: ""
#        # -- Gateway HTTP port
#        port: 80
#        # -- Gateway HTTPS port (only set when TLS is enabled)
#        # securePort: 443
#        # -- TLS configuration for the remote RGW
#        # tls:
#        #   enabled: false
#        #   # PEM-encoded certificate the operator will trust when talking to the RGW
#        #   cert: ""
#        # -- Bucket StorageClasses to create for this object store
#        storageClasses:
#          - name: "bucket" # SC name will be "<cluster>.<name>" unless fullName is set. Name must be unique across every storageclass name in the cluster.
#            # fullName: ""
#            reclaimPolicy: Delete