{{- range .Values.clusters }}
{{- if and (include "spx-ceph-conn.clusterEnabled" .) (not .local) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  namespace: {{ .name }}
stringData:
  userID: {{ .username | quote }}
  userKey: {{ .token | quote }}
{{- end }}
{{- end }}